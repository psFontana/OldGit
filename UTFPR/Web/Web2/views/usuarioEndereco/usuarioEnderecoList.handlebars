<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h2 class="mb-3">Lista de Vínculos Usuário-Endereço</h2>
            <div class="table-responsive">
                <table class="table table-striped table-hover table-bordered">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col">Usuário ID</th>
                            <th scope="col">Endereço ID</th>
                            <th scope="col">Editar</th>
                            <th scope="col">Deletar</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each usuarioEnderecos}}
                        <tr>
                            <td>{{id_usuario}}</td>
                            <td>{{id_endereco}}</td>
                            <td><a href="/usuarioEnderecoUpdate/{{id}}" class="btn btn-sm btn-outline-warning">Editar</a></td>
                            <td><a href="/usuarioEnderecoDelete/{{id}}" class="btn btn-sm btn-outline-danger">Deletar</a></td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            {{#unless usuarioEnderecos}}
                <div class="alert alert-warning text-center" role="alert">
                    Nenhum vínculo de usuário-endereço encontrado.
                </div>
            {{/unless}}
        </div>
    </div>
</div>